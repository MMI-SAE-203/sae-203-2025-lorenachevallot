---
import Hero from "../../components/Hero.astro";
import Layout from "../../layouts/Layout.astro";
import { getFilmByGenre } from "../../../backend/backend.mjs";
import CardFilm from "../../components/CardFilm.astro";
import Caroussel from "../../components/Caroussel.astro";

const genre = Astro.params.genre;

let films = await getFilmByGenre(genre);
console.log(genre);
console.log(films);
const hero = {
    title: genre || "Tous les films",
    imgHero: "",
};
---

<Layout hero={hero}>
    <Caroussel films={films} />
</Layout>
